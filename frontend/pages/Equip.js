import React from 'react';
import { StyledDiv } from './EquipWrapper.styled.js';

export const EquipWrapper = ({ children }) => <StyledDiv className="controllers-wrapper">Comming Soon!</StyledDiv>;

export default EquipWrapper;
