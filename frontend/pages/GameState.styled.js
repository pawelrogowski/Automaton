import styled from 'styled-components';

const StyledGameState = styled.div`
  color: #fafafa;
  font-size: 13px;
  padding: 20px;
  white-space: pre-wrap; /
  word-break: break-all; 
  font-family: "monospace" !important;
`;

export default StyledGameState;