import styled from 'styled-components';

const StyledList = styled.div`
  color: #fafafa;

  h3 {
    margin-top: 0;
    margin-bottom: 10px;
  }

  ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  // Styles for the list items and their contents are moved to individual item styled files.
`;

export default StyledList;
