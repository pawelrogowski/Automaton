import styled from 'styled-components';

const StyledHeader = styled.header`
  width: 100%;
  display: flex;
  align-items: center;
  z-index: 5;
  position: fixed;
  top: 0;
  left: 0;
`;

export default StyledHeader;
